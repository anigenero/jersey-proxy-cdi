language: java
jdk:
  - oraclejdk10
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
install:
  - ./gradlew clean build
after_success:
  - bash <(curl -s https://codecov.io/bash)